
export type Camion = {
  id: string;
  patente: string;
};

// Futuramente actualizable
export type CamionForm = {
  id: string;
  patente: string;
};

export type TipoCamion = {
  id: string;
  tipo: string;
};

export type Zona = {
  id: string;
  nombre: string;
  region: string;
};